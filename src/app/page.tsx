export default function Page() {
  return (
    <main style={{ padding: 40, fontFamily: 'system-ui' }}>
      <h1>Mistral Docs MCP</h1>
      <p>Unofficial MCP endpoint: <code>/mcp</code></p>
      <p>Health: <code>/api/health</code></p>
    </main>
  );
}
